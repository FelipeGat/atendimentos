RewriteEngine On

# Comente ou remova essas linhas. O PHP já está fazendo isso.
# <IfModule mod_headers.c>
#     Header set Access-Control-Allow-Origin "*"
#     Header set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
#     Header set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
# </IfModule>

# Comente ou remova essas linhas também. O PHP já trata o OPTIONS.
# RewriteCond %{REQUEST_METHOD} OPTIONS
# RewriteRule ^(.*)$ $1 [R=200,L]

# Redireciona requisições para assuntos.php se não for arquivo ou pasta
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^assuntos/?(.*)$ assuntos.php/$1 [QSA,L]
